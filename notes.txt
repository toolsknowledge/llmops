1) python3 -m venv venv

2) source venv\bin\activate (mac)
   venv\Scripts\activate (windows)

3) pip install -r requirements.txt
Ex.
        langchain
            LLM Framework
            Define LLM Flow

        pypdf
            read pdf file

        sentence-transformers
            used to create embeddings

        chromadb
            chromadb used to connect to vectordb

        openai
            used to generate AI output

        python-multipart
            used to upload the pdf files

*********************************************************************************************
Step 1.
    do changes in requirements.txt

    uvicorn[standard]
    python-dotenv

Step 2.
    create .env file

Step 3.
    modify rag.py file

Step 4.
    create .gitignore file

Step 5.
    create .dockerignore file

Step 6.
    create Dockerfile

Step 7.
    create Jenkinsfile (Pipeline)
        Checkout appln from GitHub (Stage 1)
        Build Docker Image (Dockerfile) (Stage 2)
        Push Docker Image to Docker Hub (Optional) (Stage 3)
        Deploy Docker Image to EC2 (Stage 4)

Step 8.
        push code to repo

Step 9.
        create EC2 instance

        Note : 20GB+ SSD Required

Step 10.
        assign Elastic IP (Permanent IP Address)
        Ex. 13.63.68.207

Step 11.
        Open the Below Ports
        8000
        8080
        3000

Step 12.
        update ubuntu (Refresh)

        > sudo apt update -y

Step 13.
        install Java
            Jenkins installation depending on Java

            https://www.jenkins.io/doc/book/installing/linux/

        sudo apt install fontconfig openjdk-21-jre
        java -version

Step 14.
        install Jenkins

        sudo wget -O /etc/apt/keyrings/jenkins-keyring.asc \
  https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
echo "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc]" \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update
sudo apt install jenkins

Step 15.
        install docker

        > sudo apt install docker.io -y
        > docker --version

Step 16.
    enable permissions

    > sudo usermod -aG docker ubuntu
    > sudo usermod -aG jenkins ubuntu
    > sudo usermod -aG docker jenkins

Step 17.
    restart both jenkins & docker

    > sudo systemctl restart jenkins
    > sudo systemctl restart docker

Step 18.
    connect to jenkins

    http://13.63.68.207:8080/

    password : sudo cat /var/lib/jenkins/secrets/initialAdminPassword

Step 19.
    install Below Plugins
    1) Git server
    2) Docker
    3) Docker Pipeline
    4) SSH
    5) SSH Agent
    6) SSH server
    7) AWS Credentials

Step 20.
    https://www.docker.com/products/docker-desktop/?utm_source=chatgpt.com
    username : excelr
    password : 

Step 21.
    configure Credentials in Jenkins
    1) OPENAI_API_KEY
    2) EC2_HOST ubuntu@13.63.68.207
    3) ec2-ssh-key (SSH Username w/ key)
    4) dockerhub-creds (Username with password)

Step 22.
    create pipeline


Step 23.
    download and install NodeJS
        frontend frameworks / libraries depending on NodeJS
    > https://nodejs.org/en/download
    > node --version

Step 24.
        create react application
        > npm create vite@latest

        > cd llm-rag-project
        > npm create vite@latest
            Q1: do you want to proced (y)
            Q2 : enter project name
                    frontend
            Q3: choose framework
                    react
            Q4: choose variant
                    javascript
            Q5: install libraries and run project ?
                    yes

Step 25.
        frontend
              src
                api.js
                App.jsx

        package.json
        Dockerfile
        Jenkinsfile
        nginx.conf

        it will integrated API Calls from backend to frontend
        App.jsx file is the main react component
        (it includes upload pdf files and we can query)
        (App.jsx implemented by using RWD)

        package.json file contains libraries information

Docker Compose Installation

sudo mkdir -p /usr/local/lib/docker/cli-plugins

sudo curl -SL \
  https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64 \
  -o /usr/local/lib/docker/cli-plugins/docker-compose

sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose

# Verify
docker compose version
# Must print: Docker Compose version v2.x.x
    


        
